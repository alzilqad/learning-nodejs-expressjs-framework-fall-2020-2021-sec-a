<!DOCTYPE html>
<html>
  <%- include('../header') %>

  <body>
    <div align="center">
      <form method="post">
        <legend><h2>Destination List</h2></legend>

        <div class="md-form">
          <input
            name="searchInput"
            class="form-control"
            type="text"
            placeholder="Search"
            aria-label="Search"
            style="width: 90%"
          />
        </div>
        <input class="btn" type="submit" name="search" value="Search" />
        <table class="table" style="width: 90%">
          <thead>
            <tr>
              <td>No</td>
              <td>Title</td>
              <td>Description</td>
              <td>Country</td>
              <td>History</td>
            </tr>
          </thead>
          <tbody>
            <% place.forEach( function(std){ %>
            <tr>
              <td><%= std.wishlist_id %></td>
              <td><%= std.title %></td>
              <td><%= std.description %></td>
              <td><%= std.country_name %></td>
              <td><%= std.history %></td>

              <td>
                <a href="/wishlist/<%= std.wishlist_id %>">Unsave</a> |
                
              </td>
            </tr>
            <% }); %>
          </tbody>
        </table>
      </form>
    </div>
  </body>

  <%- include('../footer') %>
</html>
